# frozen_string_literal: true

source 'https://rubygems.org'

# During development these resolve against the local monorepo build. The release
# sync (release/targets/72-varar-examples.sh) rewrites them to the published
# versions, e.g. gem 'varar-minitest', '~> 0.3'.
gem 'varar', '0.5.2'
gem 'varar-config', '0.5.2'
gem 'varar-core', '0.5.2'
gem 'varar-minitest', '0.5.2'
gem 'varar-runner', '0.5.2'

gem 'minitest', '~> 6.0'
gem 'rake', '~> 13.0'
